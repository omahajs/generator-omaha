{
  "name": "techtonic-server",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Node.js WebSocket and HTTP servers with an 80% solution for security 'baked in'",
  "keywords": [
    "websocket",
    "http",
    "server",
    "express",
    "security"
  ],
  "main": "index.js",
  "dependencies": {
    "compression": "^1.5.1",
    "config": "^1.14.0",
    "cookie-session": "*",
    "express": "^4.13.1",
    "express-session": "^1.11.3",
    "helmet": "^1.0.1",
    "lusca": "^1.3.0",<% if (markdownSupport) { %>
    "marked": "*",<% } %>
    "node-uuid": "^1.4.3",
    "npmlog": "*",
    "nsp": "*",
    "protocolify": "^1.0.2",
    "ws": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.8.1"
  },
  "scripts": {
    "scan": "nsp check",
    "prestart": "npm run scan",
    "start": "node index.js",
    "nodemon": "nodemon index.js"
  }
}
