var helpers = require('../generators/helpers');

describe('Generator helper module', function() {
    it('can add key value pairs to JSON files', function() {
        console.log('Boot!');
    });
});
